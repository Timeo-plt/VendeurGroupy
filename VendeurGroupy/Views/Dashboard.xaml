<Window x:Class="VendeurGroupy.Dashboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VendeurGroupy"
        mc:Ignorable="d"
        Title="Dashboard" Height="550" Width="700" >
        
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"  />
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" 
         Background="LightGray">
            <StackPanel Grid.Column="0">
                <Button Content="Gestion des Stocks" Margin="4" Click="Button_stock"/>
                <Button Content="Gestion des Expeditions" Margin="4" />
            </StackPanel>
        </Border>
        <Border Grid.Column="1" Padding="5" >
            <ContentControl x:Name="MainContent"/>
        </Border>
    </Grid>
</Window>
